<%= panel_with_body title: index_model_text(Ticket) do %>

    <%= link_to t('activerecord.actions.back'), raffle_path(@raffle), class: 'btn btn-primary' %>
    <%= tickets_table @initial_number, @last_number, @tickets %>
    <%= tickets_paginator @initial_number, @raffle  %>
<% end %>