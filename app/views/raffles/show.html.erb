<%= panel_with_body title: show_model_text(@raffle, :name) do %>
    <%= bootstrap_description @raffle,
                              name: @raffle.name,
                              starts_on: @raffle.starts_on,
                              ends_on: @raffle.ends_on,
                              price: human_price(@raffle),
                              tickets: @raffle.tickets.count
    %>

    <%= link_to 'Vender papeletas', raffle_sell_path(@raffle), class: 'btn btn-primary col-sm-offset-2' %>
    <%= link_to 'Tablón de ventas', raffle_assignments_path(@raffle), class: 'btn btn-primary col-sm-offset-2' %>
<% end %>